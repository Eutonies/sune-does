@using Booktex.Domain.Book.Model
<div class="sundo-space-sing">
    @foreach(var (txt, indx) in AllLines) 
    {
        <div class="sundo-space-sing-line">
            <div class="sundo-space-sing-line-name" style="width: @(Singing.Character.CharacterName.Length * 6)px;">
                @if (indx == 0)
                {
                    <div class="sundo-space-sing-line-name-text">@(Singing.Character.CharacterName)</div>
                }
            </div>
            <div class="sundo-space-sing-line-text">@(txt)</div>
        </div>
    }
</div>
